# Remaining work and resume handoff

## Active September 10 work

The user has resumed the full checkpoint goal. All current source changes are together on desktop `main`; v22 is applied locally with prompt **1.28** / policy **1.5** and remains undeployed/unqualified. Services were restored to the existing v18 deployment with the same volumes, 42 hunts, zero active jobs and unchanged database fingerprints. The preceding prompt-1.26 candidate passed 701 backend tests with explicit UTF-8 test database connections and mypy on 55 source files; related context-fitting and adaptive-reservation checks passed 48 tests. Live and analytical qualification remain open. See the current section of [resume-checkpoint.md](resume-checkpoint.md) for exact evidence, conservative fitting limits, older-checkout reconciliation, authenticated GitHub access and authorized checkpoint pushes, the corrected and verified Splunk instance restoration, and the next work. Historical pauses below no longer prevent authorized work.

The prompt-1.26 production-loop replay completed but failed analytical coverage review. The saved four-call prototype also fails chronology/count/relationship coverage. Prompt 1.27 now prefers complete final context when it fits and clarifies native identifiers versus citation labels; all 704 backend tests and typing on 55 files passed, and its separately labeled real-provider replay completed with all 447 retained representations. Analytical review still fails: omitted lead timelines, a wrong inventory missing-value count (50 versus the supplied 26), and incorrect query/source attribution. Structured coverage and claim validation remain open. These are synthesis-only diagnostics, not fresh hunts or analyst acceptance. See the active checkpoint for artifacts and measured usage.
## September 10 desktop restart status
Prompt 1.28 introduces per-question advisory lead dispositions and positive lead-endpoint citation validation, with application-owned identity/gap presentation and report preservation. Verification passed 721 backend tests, typing on 55 source files, 17 frontend tests, the production build and desktop/mobile browser checks. The new labeled analytical replay remains pending. It does not constitute a general fact checker.

**Paused at the user's request. Read [resume-checkpoint.md](resume-checkpoint.md) first.** The actual desktop source is still v21 (prompt 1.22 / policy 1.4). An unfinished v22 candidate is saved as a portable patch/source snapshot under `runtime/qualification/quality-fixes-20260910/v22-paused/`; it is not applied, deployed, committed or pushed.

The completed v21 focused diagnostic was reviewed and remains unacceptable for missing chronology, counts and relationship citation support. The saved candidate adds retained-page requests, completed-answer checkpoints, request-budget fitting, conservative duplicate provenance and query guidance. Latest focused verification: 21 new tests passed; earlier mypy passed, but the final full suite, final typing and three new end-to-end cases have not run. No new provider calls/searches/hunts were made. Application containers require WSL mount recovery; current database/job fingerprints are unverified. Preserve trial 14 and frozen fixtures. The checkpoint lists exact next steps and a known reference-label follow-up.

## Historical previous-pause record

**Paused at the user's request on September 9, 2026. Start with the [complete resume checkpoint](resume-checkpoint.md) before continuing. Work is unfinished.**

The latest local source is **v21, prompt 1.22 / SPL policy 1.4**, including the v19/v20 changes below; it is **not deployed**. Latest backend verification: **581 tests passed, no skips**, 65 focused tests, mypy 55 files. The final already-running diagnostic finished with four structurally valid question answers; factual review is pending. No new tests/model calls were started after the pause request. All 42 hunts and job fingerprints are unchanged; zero jobs are queued/claimed. Services remain on v18. No commit/push or scheduled continuation was created. The checkpoint records completed work, known failures, the inert context limit, diagnostic usage, exact source/artifact locations and the ordered remaining work.

## Historical deployment and evaluation record

The September 9 v18 image is deployed locally with a **50-search per-hunt ceiling**, the existing **20-minute execution ceiling**, **300-second model calls**, a minute-13 query/adaptive cutoff, and five minutes reserved for synthesis. GPT-5.6 Terra medium and SPL policy 1.3 remain unchanged; prompt 1.18 removes earlier assessment prose and citation-group priorities from final synthesis. Verification: **522 backend tests with isolated PostgreSQL/no skips**, mypy across 55 files, successful image build, healthy API/worker, and installed settings/source-hash checks. Deployment preserved all **41** prior hunts/jobs and both frozen fixtures. A funded baseline/candidate/control replay completed, but report completeness and an unsupported HTTPS control claim remain unresolved. Trial 14 is prepared but remains unapproved and unexecuted while the candidate evidence handoff is corrected. Earlier failed runs and discarded experiments remain preserved. Evidence availability and handoff, claim support, outage/matrix qualification, investigation efficiency, customer operating burden, and independent analyst acceptance remain open.

## Local v20 question answers and report presentation

**Historical v20 increment; candidate source only, not deployed.** Prompt contract **1.21**, SPL policy **1.4** at that increment; v21 supersedes its prompt binding. All v19 retrieval changes below remain included. [Verification](../runtime/qualification/quality-fixes-20260909/v20/verification.json).

- Every approved question has a required model answer slot containing a concise summary and supported findings or explicit limitations. Application code assigns question/finding IDs and resolves supplied citation labels. The existing bounded repair and model/time/token budgets remain. A populated slot establishes structure, not a complete or accurate answer.
- The user clarified that report length is a presentation concern. All generated findings stay in hunt results. Report drafts select ten detailed findings by default across questions, favoring hunt leads within each question; analysts may include more within the existing input-size boundary. Every question keeps its summary and full finding references. The report cannot silently remove a question or drop/reassign its finding references. PDF/HTML answer sections show summaries, limitations and counts instead of printing all internal reference IDs. PDF detailed citations are abbreviated with counts; full references remain in stored results.
- The app displays question summaries, expandable complete findings, and a link to its own retained findings/evidence section. This is not a Splunk search-results link. Browser verification used the actual production frontend build with intercepted read-only responses from the saved diagnostic; it did not mutate the application database. [UI verification](../runtime/qualification/quality-fixes-20260909/v20/report-presentation/ui-verification.json).
- Report evidence selection now considers the whole retained hunt rather than only its first 10,000 rows. Regression tests cover a cited record beyond that boundary and 2,000 retained findings without printing all references. Earlier assessment interpretations no longer reappear in limitations for reports with final question answers.
- Backend suite: **565 passed, no skips**, with isolated PostgreSQL. Frontend: **16 passed**, production build successful. Final focused report tests and mypy are recorded in the verification artifact. The earlier 561-test result predates the presentation correction and is preserved separately.
- A prompt-1.21 native-provider diagnostic supplied the same 447 saved trial-13 records and produced all four question slots and six findings on one call, no repair, 168,789 input / 2,669 output tokens. It fixed the earlier HTTPS wording and supplied the process endpoint in the communications relationship. This did not run searches or modify a hunt/fixture. [Diagnostic](../runtime/qualification/quality-fixes-20260909/v20/question-summary-live/verification.json).
- **Coverage review still fails.** q2 describes the exact-hash lead but omits other lead timelines. q3 focuses the detailed authentication chronology on that lead. q4 omits requested distinct entity counts and selects four pre-execution communications even though later records were supplied. Broad-query truncation limits a complete inventory but does not justify omitting scoped retained counts or available later observations. Do not call this overall qualification. [Assistant review](../runtime/qualification/quality-fixes-20260909/v20/question-summary-live/quality-review.json).
- Next: implement automatic retrieval of retained evidence by question/entity/time, timeline-aware selection, scoped entity counts and duplicate handling; fix restrictive general timeline queries and null-dropping authentication aggregation; size model contexts by tokens as well as rows. Then deploy a coherent candidate and qualify fresh plans against unchanged fixtures. Preserve unapproved trial 14 and all historical trials. Outage/full-matrix and independent analyst acceptance remain open.

## Local v19 work after the v18 deployment

**Candidate source only; no v19 image or deployment yet.** Prompt contract 1.19 / SPL policy 1.4. The current candidate passes **555 backend tests with isolated PostgreSQL and no skips**, mypy across 55 source files, and `git diff --check`. [Verification](../runtime/qualification/quality-fixes-20260909/v19/verification.json).

- Raw representative/targeted query ceilings are now 10,000; aggregate remains 500. The executor fetches fixed pages of at most 500 rows and enforces remaining query/hunt row and byte capacity. It preserves order, accounts for one search across pages, distinguishes server output counts from retained counts, and records page counts and retrieval stop reasons. Known totals resolve exact-cap completeness; unknown totals remain conservatively truncated. Prior pages survive later timeout/byte failures. Empty partial retrieval is not labeled zero matches. Budget-skipped searches are checkpointed and reported.
- Page body reads now run inside the bounded adapter call, with timeout clipped to remaining query time. Body-read exceptions propagate instead of becoming empty successful results. Tests explicitly release and observe a timed-out underlying read; caller timeout does not magically stop that reader.
- Native source/time/event identity fields are retained in raw projections; removing or overwriting them is rejected. The query record stores executed normalized SPL. Citation enums share schema definitions so 500 supplied evidence references fit the provider schema limit; original citation/reference constraints remain.
- Model evidence batches now default to 500 records. Assessment uses the existing representative-event setting; pivot planning and synthesis use the targeted-event setting. Repairs keep their original sample. A deterministic replay supplies all 447 trial-13 records with zero model-sample omissions; this makes no model call and does not prove report accuracy. [Replay](../runtime/qualification/quality-fixes-20260909/v19/batch500-trial13-replay.json).
- Real Splunk verification retained all 390 results from the unchanged old q4 search (previously 100) and exactly all 594 frozen DNS/network event IDs from a broader scoped query across two pages, with no duplicates and native provenance present. Both searches were read-only; no hunt or fixture was modified. The initial diagnostic verifier used the wrong fixture event-ID path; its failed log is preserved and saved results were verified offline without rerunning the searches. [Live verification](../runtime/qualification/quality-fixes-20260909/v19/paging-live/verification.json).
- The earlier question-focused prototype is invalid: it filtered evidence while retaining full query counts, creating artificial retention-gap metadata. Preserve it as a prototype failure, not a model-only failure. The separate required-question-slot diagnostic produced six contract-valid findings including chronology/authentication/communications, but factual review failed: its communications title asserts HTTPS from TCP/443, and its session-to-execution relationship lacks a process citation in that finding. Neither prototype is deployed. [Question-slot review](../runtime/qualification/quality-fixes-20260909/v19/question-contract-diagnostic/quality-review.json).
- Next: implement retrieval of retained evidence by question/entity/time, improve timeline selection and duplicate handling, correct restrictive query construction/null-dropping aggregation, and verify semantic question coverage (required answer slots are now implemented in v20 above). Context sizing must respect token capacity as well as row count. Then build/deploy a coherent candidate and prepare a fresh plan; preserve unapproved trial 14 and all earlier hunts. Full scenario/outage/independent analyst/release gates remain open.

## Earlier execution steps (superseded by the resume checkpoint)

1. Read this handoff and the [quality review](../runtime/qualification/live-e2e-20260908/quality-review.json).
2. Deterministic report fixes are implemented: exact source/provenance text, distinct cited counts, classification totals, completed-search sources, and deduplicated citations. Recheck these against the candidate live report.
3. Approved-question scheduling now prioritizes unsearched approved questions and continues after explicitly skipped batches. Verify actual coverage and claim support in the candidate run; prompt changes alone do not prove factual correctness.
4. The deployed v15 candidate passed **500 backend tests with isolated PostgreSQL/no skips**, mypy across 55 source files, image build, and installed-code replay. Its deployment preserved all 40 existing hunts and job records. The unchanged frontend previously passed 14 tests and its production build. Boolean source validation, query-window context, and evaluation identity/terminal-state corrections are implemented and verified below.
5. Scoped discovery and the fresh model plan are corrected and verified against live Splunk. Preserve trial 2's misleading unapproved plan. Trial 3 completed but failed coverage review. Its report accounting and 16/16 indicator retention/citation recovery are verified separately from that failure. Trial 4 is now a preserved failed run. The three diagnosed corrections below are deployed. Trial 5 failed entity assessment after retrieving all 16 indicators. The entity correction is deployed in v7; trial 6 completed after an explicit plan edit/review. V8 source corrections and v9 time-bound/deferral corrections are now deployed. Preserve failed-quality trial 7; preserve trial 8 and unapproved trial 9. Preserve reviewed trials 10 and 11 and all separate model diagnostics. Fresh v14 trial 12 completed and failed factual review; preserve its draft. V15 corrects the result-kind defect; relationship/claim support still needs correction before further qualification. The realistic fixture is frozen and loaded under the user criteria below. Compare actual enforced scope, coverage, citation recovery, factual accuracy, first-pass validity, and resource usage.

## Current deployed candidate

| Item | State |
| --- | --- |
| App | http://localhost:8080/ |
| Backend and worker | `threat-hunting:local-20260909-quality-v18-231548` |
| Frontend | `threat-hunting-frontend:local-20260908-hardening` |
| Database | PostgreSQL 16; migration `0013_hunt_listing_index` |
| Model | OpenAI `gpt-5.6-terra`, reasoning `medium` (v16 evaluation); prompt contract `1.18`; SPL policy `1.3`; runtime image `local-20260909-quality-v18-231548` |
| Execution | Direct Splunk adapter; local demo disabled; 20-minute hard ceiling, minute-13 query/adaptive cutoff, 300-second model calls, 50-search ceiling |
| Runtime recovery | Docker Desktop engine recovered September 9 by preserving/replacing stale socket directories; Docker server 29.7.2 responds. API/worker/PostgreSQL and the lab Splunk on port 18089 are healthy; app readiness returns HTTP 200. |
| Work in progress | Trial 1 `16f13628-de1d-4155-85ed-5f34fc85a652` is terminal `failed`; never resume its job. Trial 2 `c7fbde8b-80b9-42a8-ade8-91382e758ecb` is `awaiting_plan_review`, with no approval or execution requested. See its `plan-review.json` before any further action. Trial 3 `2b2efabe-934d-47a6-b5cf-083403407d53` is terminal `report_draft` and failed coverage review. Preserve it. Trial 4 `60cde17e-98e7-4b1e-af73-d907656b80dc` is terminal `failed`; preserve it. V6 deployment preserved all 32 previous hunts and jobs. Trial 5 `a9602750-2dd5-4288-81d4-1857b6077496` is terminal `failed`. Preserve it. V7 deployment preserved all 33 existing hunts and jobs. Trial 6 `86aeae6c-fdff-474d-a33e-cf62b6261567` is terminal `report_draft`; its job `17e31d61-7759-4719-99b4-e25e7e22f3ec` is completed. Preserve it. Trial 7 `3f321542-07f1-4ef6-ad62-0ff421e52a98` is terminal `report_draft` and failed quality review; job `d77da81a-dd97-4d3a-a001-dafa3fde8f91` is completed. V9 preserved all 35 hunts and jobs. V11 preserved 37; v12 preserved 38. Trial 8 is completed `report_draft` but failed quality review; trial 9 is unapproved `awaiting_plan_review`. Trial 10 `9a1a6270-5e28-4583-8a81-7ad40fe23e30` is completed `report_draft`; job `3bc52f6c-2794-4d03-909a-6925738f8039` is completed. Trial 11 `f1592176-13cd-4f9b-9140-e38d3b4b6b10` is completed `report_draft` and failed factual review. V14 preserved 39 hunts/jobs with no active execution jobs before trial 12 preparation. Trial 12 `5b549b2d-400f-42c2-93eb-c6074eee0e36` is completed `report_draft` and failed factual review. V16 model rollout preserved all 40 prior hunts/jobs. Trial 13 `e98ad766-9dc8-4ab6-a727-ec6a3cf6b72a` is terminal `report_draft`; its primary source relationships pass review, but report completeness fails. Preserve it. V17 deployment preserved all 41 hunts/jobs. The user restored API funding. V18 passed a funded context comparison at the provider/contract level, but completeness and an unsupported HTTPS control claim remain unresolved. V18 is deployed and healthy with a 50-search ceiling; all 41 prior hunts/jobs and their approval snapshots survived unchanged. Trial 14 is prepared but remains unapproved/unexecuted while the evidence handoff is evaluated. |
| Working tree | Extensive uncommitted changes from this and earlier work; preserve them. No commit/push performed. |

The local lab retains its explicit insecure-TLS and HTTP-cookie overrides. This is not a production-security qualification.

## Completed before the September 8 live hunt

- Updated existing engineering/LLM skills and rules; no new skill required for the original fixes.
- API-enforced model schemas; request-local citation labels resolved by application code; prior assessments and supporting evidence passed to synthesis; application-computed evidence time bounds.
- Atomic hunt-state/audit/enqueue transaction; PostgreSQL active-hunt admission fencing; bounded model work and transport deadlines; application-scoped dependencies; upload offloading; paginated summary listings; incremental workflow/report separation.
- Locked Python dependencies, backend CI configuration, typing corrections, real PostgreSQL migration/concurrency tests, safe diagnostics, local rollout, and retained rollback images.
- Verified **348 backend tests**, including PostgreSQL checks with no skips; **14 frontend tests**; mypy across **55 source files**; frontend production build; lock and diff checks.
- Earlier real-model two-step temporal regression passed. These automated/regression results do not establish general investigation quality.

Evidence: [hardening verification](../runtime/qualification/hardening/verification.json), [deployment verification](../runtime/qualification/hardening/deployed-verification.json), [backend test log](../runtime/qualification/hardening/backend-final.txt).

## September 8 completed live test (failed quality baseline)

| Item | Observed result |
| --- | --- |
| Hunt | `50918288-674f-4595-b769-67acf29d5afa` |
| Title | Live verification: Play indicator investigation — 2026-09-08 |
| Job | `abe2ce81-d3ea-4225-90a5-a256e6e8b226`; completed on attempt 1 |
| Final hunt state | `finalized`; finalization tested delivery and does not mean quality acceptance |
| User flow | API create/discovery; real browser sign-in, plan review/approval, execute, report review, save/finalize, PDF download, and sign-out |
| External execution | Real deployed PostgreSQL/worker, Splunk searches, and OpenAI calls; no mocked model or Splunk results |
| Scope | 2026-01-01T00:00:00Z through 2026-01-05T00:00:00Z; four explicit `th_test_*` lab indexes |
| Fixture | 776 versioned advisory events verified against current Splunk content; scoped snapshot has 2,088 synthetic events total |
| Queries | 6 completed; 0 failed; endpoint/auth only |
| Model calls | 1 planning + 12 execution; 0 repairs; execution schema/citation-reference checks all passed first try |
| Tokens | 253,098 input / 13,789 output including planning; monetary cost not measured |
| Evidence | All 328 retained records matched their actual Splunk job result rows |
| Expected indicators | 16/16 retained; 13/16 cited by findings = **81.25%**, below the documented 90% recovery threshold |
| PDF | 8 pages; parsed and first page visually checked; browser download matches API bytes |
| Safety/state checks | CSRF rejection, approved-plan immutability, duplicate-execution rejection, finalized-report immutability, logout revocation, audit trail, and one execution job verified |
| Existing data | All 27 pre-existing hunts unchanged; new test hunt belongs to a dedicated test account |

The initial browser timeout was an exact-label selector error in the temporary test script, corrected before execution. Subsequent browser stages reported zero page errors. The initial fixture-marker search returned zero because this ingestion lacks `synthetic_run_id`; subsequent verification compared the actual event fields against the versioned fixture and matched all 776. Neither initial attempt is a successful application qualification result.

## Issues found by the live test and current correction status

| Priority | Finding | Next correction |
| --- | --- | --- |
| High | Report limitation cleanup replaces `synthetic` with `available`, corrupting exact names such as `synthetic:process` and `synthetic_event_id`. | Implemented and regression-tested: preserve source identifiers and synthetic-data provenance. See `_concise_report_content` in [reports.py](../src/threat_hunting/services/reports.py). |
| High | Report `data_sources_used` lists planned DNS/network sources even though neither was searched. | Implemented and regression-tested: derive usage from completed queries, including zero-result searches, and disclose missing coverage. See `_execution_report_content` in [reports.py](../src/threat_hunting/services/reports.py). |
| High | Report says findings cite 328 records; they cite **68 distinct** records. It calls all 3 findings hunt leads although 2 are `supported_observation`. | Implemented and regression-tested: distinct cited counts and classification totals remain separate from retained counts. |
| High | Follow-up execution consumed the model budget on endpoint/auth pivots and never investigated approved DNS/network question `q4`. | Implemented and regression-tested: approved questions precede pivots; skipped batches preserve capacity for later questions; pending IDs are checkpointed. Live coverage verification remains. Inspect `_draft_execution_ledger` and `_assess_execution_round` in [workflow.py](../src/threat_hunting/services/workflow.py). |
| Medium | A finding states Q1 and Q2 were truncated at 100 rows; Q1 actually completed with all 20 rows. | Query truncation and model-sample omission are now separate context facts; synthesis is instructed to leave counts/completeness to application metadata. Factual claim quality still needs live review. |
| Medium | One finding correlates auth with endpoint execution but cites only authentication records. Three expected indicator events are retained but absent from finding citations. | Synthesis instructions now require support for each material claim and each source in a correlation. The observed evaluator separates retrieval/citation recall. Live claim review remains; do not pad irrelevant citations for a score. |
| Medium | Two findings repeat evidence IDs (1 and 10 duplicates). | Implemented and regression-tested: citation references are deduplicated in application code while preserving deterministic order. |

Review provenance is **assistant**, not an independent human analyst. Correct UUID references and schema-valid output did not make the report factually correct.

## September 9 verification and evaluation work

- Backend tests: **376 passed, no skips**, using an isolated PostgreSQL 16 database rather than the application database. See [test result](../runtime/qualification/quality-fixes-20260909/backend-tests-result.json) and [test log](../runtime/qualification/quality-fixes-20260909/backend-tests.txt).
- A new scheduling regression first reproduced early termination after a skipped batch, then passed after the fix. A round without unassessed results reserves only its required planning call plus synthesis, without increasing any configured budget.
- The observed evaluator records scenario/variant/trial identity, fixture and runtime versions, separate retrieval/citation recall, approved-question execution coverage, and observed variation across repeated trials. Unknown expectations remain distinct from explicitly empty expectations.
- The maintained [live-trial API runner](../scripts/run_live_hunt_trial.py) prepares a new hunt, requires the reviewed plan hash for approval/enqueue, and captures actual results/configuration. Tests cover answer-key isolation, scope expansion, duplicate execution, and origin constraints. It does not load fixtures or replace the full twelve-scenario adapter.
- README, specification, and operator runbook describe deployed prompt contract **1.10** and the [repeatable-trial procedure](operator-runbook.md#repeatable-live-trials).
- Candidate image build, deployment, and an actual rollback/return rehearsal passed. All 28 pre-existing hunts and job state were unchanged across both releases. See [deployment verification](../runtime/qualification/quality-fixes-20260909/deployment-verification.json) and [rollback rehearsal](../runtime/qualification/quality-fixes-20260909/rollback-rehearsal.json).
- Fresh live comparison, independent analyst acceptance, and the release items below remain open. Automated tests do not establish those outcomes.

## New findings from candidate trial 1

- All four approved question IDs received completed searches, including a zero-result DNS/network search. Eleven searches completed; no Splunk query failed.
- Two assessment batches selected question IDs inconsistent with their queries. The application already knows that relationship; move this identity mapping out of model output while preserving canonical stored contracts.
- Final synthesis produced a schema/reference-invalid response on model call 12. The attempted repair was correctly blocked by the existing call limit, but diagnostics lost the original validation reason. Preserve safe validation codes before a repair can fail; do not increase budgets to conceal the cause.
- Report source derivation cross-products indexes and sourcetypes from a proposal. q4's actual SPL has paired DNS/network alternatives. Represent grouped query scope accurately without inventing source pairs.
- Preserve [failed trial artifacts](../runtime/qualification/quality-fixes-20260909/trial-1/quality-review.json). A separate prompt-1.3 synthesis diagnostic reproduced a citation relationship failure; the original response remains unavailable. A prompt-1.4 diagnostic validated on its first call, but neither diagnostic constitutes a completed hunt or analytical acceptance. No candidate report/PDF was produced.

## Synthetic-data assessment and newly reproduced query defect

The September 9 user-requested assessment inspected all 2,088 saved scoped events and replayed the actual q4 search against Splunk. See the [assessment evidence](../runtime/qualification/quality-fixes-20260909/synthetic-data-assessment.json) and [controlled query replay](../runtime/qualification/quality-fixes-20260909/trial-1/q4-boolean-precedence-diagnostic.json).

- **q4's zero results do not establish a telemetry gap.** Its compound source alternatives lack individual parentheses. Splunk `search` evaluates OR before AND. Adding only the missing parentheses, with the same hosts, timestamps, data, and projection, changes zero results to **105 events: 57 DNS and 48 network**. Query completion and schema validity did not prove query correctness. Correct query construction/validation and add a behavior regression before the next qualification run.
- q4 also narrows its window to January 2 at 00:00 through January 3 at 01:00 UTC. The fixture's 28 staged DNS/network events occur January 3 from 02:00 through 03:45, inside the approved hunt scope but outside this query. Review pivot-window selection and coverage reporting; a completed search is not an answered question.
- The data has material realism limitations: **175/190** advisory background process events have different `process` and `file_name` values, all 16 indicator events are exactly 17 minutes apart, and seven use synthetic `play-loader-*` names. Six other scoped events expose known-answer case metadata. These can bias interpretation, but their causal contribution to model failures has not been measured with a controlled data-only comparison.
- All 16 selected indicator hashes have valid lengths and match the saved advisory STIX; all 16 were retained in trial 1. The fixture remains useful for reproducible retrieval and workflow regressions. Its controlled artificial patterns and sparse background do not qualify general investigation quality.
- Evaluation must distinguish fixture event identity from Splunk identity. `source_event_ref` can contain Splunk `_cd`; use an explicitly declared fixture identity projection when scoring expected `event_id` values. Inspecting `selected_result.event_id` confirms all 16 indicators were retained in trial 1 and cited in the **separate** prompt-1.4 diagnostic. This does not change trial 1's failed state. The maintained scorer still needs this correction, and failed trials must remain represented in comparison exports.
- Preserve this fixture and failed runs. Add a separately versioned dataset with coherent entity/time relationships and analyst-visible fields that exclude answer labels; include both supported investigations and deliberately incomplete coverage. Do not rewrite the baseline to make an existing run pass.

The prompt-1.4 candidate `threat-hunting:local-20260909-quality-v2-172930` was built and tested but never deployed. Its assessment-identity, citation-shape, safe-error-code, and grouped-source corrections are included in deployed v3.

## V3 corrections, rollout, and discovery issue found during plan review

- **404 backend tests passed, no skips; mypy passed across 55 source files.** See [verification](../runtime/qualification/quality-fixes-20260909/v3/verification.json). The new source-policy tests first reproduced rejection failures, then passed after implementation.
- SPL policy 1.2 proves positive index/sourcetype bounds across Boolean branches using Splunk `search` precedence. It rejects contradictory and unbounded source expressions before execution. [Replaying trial 1's proposals](../runtime/qualification/quality-fixes-20260909/v3/policy-replay.json) rejects the original q4, accepts its parentheses correction, and accepts the other ten proposals. This proof does not establish arbitrary SPL semantics or analytical usefulness.
- Actual query time bounds now reach assessment, adaptive planning, and synthesis; historical assessment/synthesis contexts recover them from the ledger. Narrow search windows produce explicit report limitations. Guidance surveys the approved range before narrowing and does not equate zero results with full-range absence. Live investigation behavior still needs qualification.
- Evaluator-only `fixture_event_id_field` selects the retained-result identity explicitly. Failed/cancelled hunts are exported with `terminal_state` and remain in completion denominators. The fresh read-only [trial 1 export and score](../runtime/qualification/quality-fixes-20260909/v3/trial-1-scores.json) preserves its failed status, correctly reports 16/16 retrieved, and reports zero final citations because that hunt produced no findings. Separate synthesis diagnostics remain separate.
- V3 image `threat-hunting:local-20260909-quality-v3-174949` is built and deployed. API and worker are healthy; HTTP readiness is 200; all 29 pre-existing hunts and job records survived unchanged. [Deployment evidence](../runtime/qualification/quality-fixes-20260909/v3/deployment-verification.json). Protected backup: `runtime/qualification/quality-fixes-20260909/v3/pre-deploy-database.dump`; prior environment binding: `v3/prior-deployment.env`. The image and runtime snapshots now bind the actual policy constant instead of the stale `2026-01` label.
- All 2,088 scoped Splunk events were reverified unchanged immediately before trial 2 planning. The new plan correctly binds prompt 1.5, policy 1.2, the v3 image, and the original four-day/index scope.
- **Do not execute trial 2's current plan.** Its network-field limitation is false for the hunt scope. Bootstrap discovery uses one recent event and the index's latest seven days. Network telemetry extends to January 12 because of other fixtures, so discovery searches January 5–12, outside this hunt's January 1–5 interval. [Controlled searches](../runtime/qualification/quality-fixes-20260909/v3/discovery-sampling-diagnostic.json) show both `dest_ip` and `dest_port` absent in that discovery window but present when only the window is changed to the hunt scope, even with the same one-event cap. This is an application sampling defect triggered by mixed fixture dates; increasing the sample cap alone does not correct it.
- This discovery correction is implemented and deployed in v4 below. Trial 2 remains unchanged as evidence of the original failure.

Trial 2 [plan review](../runtime/qualification/quality-fixes-20260909/trial-2/plan-review.json) records why approval/execution were deferred. This is an engineering correction in progress, not a request for user permission. Trial 3 is now a preserved terminal report draft; never resume or approve the preserved trial 1/trial 2 records.

## V4 scoped discovery correction

- The planning flow now reads catalog metadata, proposes a scope, samples its exact index/sourcetype pairs within the proposed dates, and conditionally revises the plan with the refreshed field context. The final model response cannot change the sampled scope. The saved plan references the refreshed immutable discovery snapshot.
- Sampling uses at most 100 events per pair and eight pair searches. Ranges over seven days sample their final seven days, still inside the proposed scope; both requested and sampled bounds are explicit. Field samples are nonexhaustive. Exact source pairs avoid spending the search budget on a cross-product of unrelated sources, and repeated sourcetypes across indexes each receive their own sample.
- Analyst scope edits refresh discovery without a model rewriting their question text. A failed refresh preserves the prior plan and snapshot. Approval rejects a mismatched scope binding. Existing historical plans remain preserved; generate and review a fresh plan for prompt contract 1.6.
- **412 backend tests passed with no skips**, including isolated PostgreSQL checks; mypy passed across 55 source files. See [verification](../runtime/qualification/quality-fixes-20260909/v4/verification.json). New regression tests cover historical scope, scope edits, immutable snapshot binding, rejected model scope changes, and failure atomicity.
- A [read-only live Splunk check](../runtime/qualification/quality-fixes-20260909/v4/scoped-discovery-verification.json) recovered network `dest_ip` and `dest_port` within January 1–5 for the original four source pairs. No model call or hunt mutation was needed for that diagnostic. All [2,088 fixture events still match](../runtime/qualification/quality-fixes-20260909/v4/fixture-verification.json) the frozen baseline.
- [Deployment](../runtime/qualification/quality-fixes-20260909/v4/deployment-verification.json) is healthy, readiness returns 200, and all 30 pre-existing hunts and jobs are unchanged. V4 image: `threat-hunting:local-20260909-quality-v4-181510`; prompt contract 1.6; policy 1.2. Protected database backup and prior environment binding are under `runtime/qualification/quality-fixes-20260909/v4/`. No migration change was required.
- Fresh trial 3 `2b2efabe-934d-47a6-b5cf-083403407d53` completed planning in two model calls without repair (15,140 input / 3,742 output tokens). Its [reviewed plan](../runtime/qualification/quality-fixes-20260909/trial-3/plan-review.json) preserves the four-day/four-source scope, uses observed network destination fields, and no longer asserts the false missing-field limitation. Plan SHA-256: `1e3d643aea3fc4fd59ee05b8cf25e4f866f932ac9b68b40ed2def205c1ac1c62`. Approval and execution completed through the normal API. Its terminal quality review and the next correction are recorded below. Independent analyst acceptance remains open.

## Trial 3 review and v5 follow-up repair

- Trial 3 completed to `report_draft` in five execution model calls (one repair), with 91,451 input / 7,066 output tokens. It ran only q1: one search, 20 retained rows, one supported observation, 20 distinct citations, no citation duplicates. **Coverage failed: q2, q3, q4 were not searched.** See [quality review](../runtime/qualification/quality-fixes-20260909/trial-3/quality-review.json).
- The original batch contained distinct, valid q2/q3/q4 proposals plus one generated pivot that duplicated q2. The old validator attributed that single duplicate error to all four proposals. The repair then skipped all four. A [replay against the corrected validator](../runtime/qualification/quality-fixes-20260909/trial-3/duplicate-rejection-replay.json) rejects only the generated pivot. This is application validation/repair behavior, not a synthetic-data realism failure.
- V5 attributes each validation error to its own proposal, repairs only rejected decisions, and preserves accepted proposals unchanged. The merged batch is revalidated; an invalid repaired query cannot execute. Regression tests first reproduced the broad rejection, then verified preservation of all three valid approved proposals and rejection of a still-invalid repair. **414 backend tests and mypy across 55 files pass.** [Verification](../runtime/qualification/quality-fixes-20260909/v5/verification.json).
- Report accounting is now correct: 20 distinct citations / 20 retained rows / one search / one supported observation. Only endpoint is listed as used; unsearched questions and planned sources are disclosed. A remaining presentation issue is request-local E labels appearing in free-text finding limitations.
- The result projection omitted `event_id`. The original [offline score](../runtime/qualification/quality-fixes-20260909/trial-3/scores.json) reports 20 unidentified records; its zero recall is not evidence that IOC events were missed. A separate [frozen-fixture projection reconciliation](../runtime/qualification/quality-fixes-20260909/trial-3/fixture-projection-reconciliation.json) uniquely matches every projected row using its retained content and source. It verifies all **16/16 expected events were retained and cited**, without modifying the hunt or original score. The 16 advisory rows expose multivalue `host` containing HEC metadata and the raw host; the reconciliation documents membership matching explicitly. A maintained evaluation identity solution is still needed when projections omit source IDs.
- V5 `threat-hunting:local-20260909-quality-v5-182604` is [deployed and healthy](../runtime/qualification/quality-fixes-20260909/v5/deployment-verification.json), prompt 1.7 / policy 1.2. Readiness is 200; all 31 pre-existing hunts and jobs are unchanged; migration remains 0013. Protected backup and prior deployment environment are in `v5/`. All [2,088 scoped fixture events remain unchanged](../runtime/qualification/quality-fixes-20260909/v5/fixture-verification.json).
- Fresh trial 4 `60cde17e-98e7-4b1e-af73-d907656b80dc` passed plan review and was approved/executed through the normal API. It is now terminal `failed`; do not reuse or mutate it. Its failure is detailed below.

## Trial 4 failure and diagnosed corrections

- Trial 4 failed after four execution model calls, including one follow-up repair (60,706 input / 6,804 output tokens). Only q1 executed, retaining 13 rows; no findings were produced. [Quality review](../runtime/qualification/quality-fixes-20260909/trial-4/quality-review.json). The v5 error-attribution correction worked: only the generated pivot received the duplicate semantic error. Other proposals had separate SPL-policy errors.
- All follow-up proposals began with a grouped implicit search. The existing canonicalizer accepted `index=...` but rejected `(index=...)`. A [controlled Splunk API check](../runtime/qualification/quality-fixes-20260909/trial-4/implicit-search-diagnostic.json) confirms the bare grouped expression receives HTTP 400, while adding explicit `search` returns a bounded result. Local policy **1.3** now normalizes grouped initial expressions, preserving exact source-branch, command and time guards. [Replay](../runtime/qualification/quality-fixes-20260909/trial-4/policy-1.3-replay.json) accepts all four original proposals at the SPL-policy layer. The duplicate pivot remains a separate decision error.
- The repaired generated authentication pivot still duplicated q3. The workflow failed before executing follow-ups. **Resolved in deployed v6 below:** application duplicate suppression preserves distinct approved work, validation and explicit limitations. Do not simply relax execution guards or retry the failed job.
- Initial q1 assumed `file_hash_type="sha1"` / `"sha256"`; actual records use `"sha_1"` / `"sha_256"`. The [controlled comparison](../runtime/qualification/quality-fixes-20260909/trial-4/hash-type-predicate-diagnostic.json) changes only these literals and recovers 20 rows instead of 13. [Offline projection reconciliation](../runtime/qualification/quality-fixes-20260909/trial-4/fixture-projection-reconciliation.json) confirms the original run retained only 9/16 indicator events; the seven missing rows are the hash-only loader records. **Resolved in deployed v6 below:** query context and guidance avoid unnecessary type predicates for exact hash matching and require grounded categorical values. Do not hardcode fixture-specific aliases or declare the data unrealistic to explain this failure.
- The local offline scorer now reports `null` recall and missed-event lists when missing identities could change them; identified match counts remain explicit, and proven citation/complete recovery remains measurable. Updated exports preserve the originals: [trial 3 identity-aware score](../runtime/qualification/quality-fixes-20260909/trial-3/scores-identity-aware.json), [trial 4 identity-aware score](../runtime/qualification/quality-fixes-20260909/trial-4/scores-identity-aware.json). Trial 4 has zero final citations because it produced no findings; its retrieval recall is unknown by direct identity and measured separately by frozen-fixture projection reconciliation.
- The initial local v6 normalization/scorer increment passed 422 backend tests. The completed v6 candidate and its stronger verification are recorded below. Preserve all failed exports and original fixture data.

## V6 deterministic duplicate handling and grounded hash queries

- Application code suppresses follow-up searches only when normalized SPL, UTC bounds, result mode, limits and execution configuration match. Approved-question priority is preserved even if the model returns decisions in another order. Duplicate skips are explicit application decisions; they do not consume model repair and do not claim the question was separately searched. Other semantic and policy failures remain validated and observable.
- The [replay of trial 4's actual proposals](../runtime/qualification/quality-fixes-20260909/v6/trial-4-follow-up-replay.json) preserves q2/q3/q4 and skips only the duplicate authentication pivot, with zero repairs. Tests also verify that changed time windows or result limits remain new work.
- Advisory query context supplies literal `file_hashes` without algorithm-key names. Guidance uses exact values in shared hash fields without additional type filters and grounds other categorical predicates in supplied or observed context. No fixture-specific hash aliases were added.
- A separate [real-model initial-query diagnostic](../runtime/qualification/quality-fixes-20260909/v6/initial-query-diagnostic.json), using the preserved trial 4 plan, passed first try with one model call and no repair. Its [actual Splunk search](../runtime/qualification/quality-fixes-20260909/v6/initial-query-splunk-verification.json) returned 20 rows and all 16 expected indicator events. This diagnostic is not a completed hunt or analyst acceptance.
- [Fixture analysis](../runtime/qualification/quality-fixes-20260909/v6/fixture-hash-type-distribution.json) confirms a data inconsistency correlated with the positive records: all 16 expected indicators use underscored algorithm labels (15 `sha_256`, one `sha_1`), while 190 other process records use `sha256` and eight have no type. This contributed to the type-filter failure; it does not excuse unsupported categorical assumptions or explain unrelated workflow failures. A coherent, blinded fixture and representative independent evaluation remain necessary.
- **425 backend tests passed with no skips**, using isolated PostgreSQL; mypy passed across 55 files and the image build passed. [Verification](../runtime/qualification/quality-fixes-20260909/v6/verification.json). [Deployment](../runtime/qualification/quality-fixes-20260909/v6/deployment-verification.json) uses `threat-hunting:local-20260909-quality-v6-185214`, prompt 1.8 and policy 1.3. API and worker are healthy, readiness is 200, and all 32 prior hunts and job records are unchanged. Protected backup/prior environment are under `v6/`; no migration changed.
- All [2,088 scoped fixture events remain unchanged](../runtime/qualification/quality-fixes-20260909/v6/fixture-verification.json). Fresh trial 5 `a9602750-2dd5-4288-81d4-1857b6077496` passed [plan review](../runtime/qualification/quality-fixes-20260909/trial-5/plan-review.json) and executed through the normal API; it is now terminal `failed`. Its approved plan SHA-256 is `45c58d49dbb0e922dc0d7ed50db7d7e4f0ad7324e6381d56a885a9d201638ed6`. Its artifacts are under `runtime/qualification/quality-fixes-20260909/trial-5/`; job `08081592-c27d-4672-b182-2482530303e6` is terminal `failed` and must never be reenqueued. Analytical quality remains unqualified.

## Trial 5 entity-contract failure and v7 corrections

- Trial 5 retained 20 rows and all **16/16 expected indicators** with a valid first-pass q1 search. It then failed assessment after one repair: three execution model calls, 37,124 input / 5,787 output tokens. No findings were produced and q2/q3/q4 were not searched. [Quality review](../runtime/qualification/quality-fixes-20260909/trial-5/quality-review.json), [observed scores](../runtime/qualification/quality-fixes-20260909/trial-5/scores.json). The follow-up scheduling fixes were not exercised by this run.
- Both the initial and repaired assessments contained 49 entities, of which 15 were file paths extracted from longer quoted command lines. Those paths are present as substrings, but the authoritative entity contract accepts complete scalar field values or list elements. The old instruction said only “appear exactly in the row,” and the generic rejection did not identify the failing entities. This is a contract/repair mismatch; the quoted command-line inputs are legitimate and do not explain the failure as unrealistic synthetic data.
- Local prompt **1.9** clarifies the exact scalar rule in assessment guidance and the native response field description. Validation now identifies all failing zero-based `new_entities` positions, without echoing source values. Exact observed filenames and multivalue host elements remain accepted; inferred substrings remain rejected. Useful command-line interpretation belongs in grounded summary text. No entity is silently removed or normalized.
- The separate [prompt 1.9 diagnostics](../runtime/qualification/quality-fixes-20260909/v7/assessment-diagnostics.json) repaired the saved 15-entity failure in one call, but a fresh first-pass response still attached a real hostname to the wrong rows. This remains recorded as a failure. The selected hostname exists elsewhere in the same supplied query result.
- Local prompt **1.10** moves entity row-reference lookup into application code. The model selects type and exact observed value; the application derives matching references only from the selected query's supplied rows. Historical/canonical references remain validated. The [saved-response replay](../runtime/qualification/quality-fixes-20260909/v7/entity-reference-replay.json) preserves all 48 selected values and their query, and validates after deriving the row relationships. It makes no model call and does not rewrite the failed hunt.
- The separate native-provider [prompt 1.10 first-pass diagnostic](../runtime/qualification/quality-fixes-20260909/v7/entity-reference-diagnostic/assessment-diagnostics.json) validated all 49 selected entities with application-derived citations in one model call, without repair. This is not a completed hunt or analyst acceptance.
- **430 backend tests passed with no skips**, including isolated PostgreSQL; mypy passed across 55 files and image build passed. [Verification](../runtime/qualification/quality-fixes-20260909/v7/verification.json). The [v7 deployment](../runtime/qualification/quality-fixes-20260909/v7/deployment-verification.json) is healthy with readiness 200 and all 33 previous hunts/jobs unchanged. Image: `threat-hunting:local-20260909-quality-v7-190952`, prompt 1.10 / policy 1.3. Protected backup and prior environment are under `v7/`; no migration changed.
- All [2,088 scoped fixture events remain unchanged](../runtime/qualification/quality-fixes-20260909/v7/fixture-verification.json). Trial 6 `86aeae6c-fdff-474d-a33e-cf62b6261567` completed to `report_draft` under `runtime/qualification/quality-fixes-20260909/trial-6/`. Its [plan review](../runtime/qualification/quality-fixes-20260909/trial-6/plan-review.json) required an edit: the generated plan invented an advisory reference and proposed fixture-answer grouping. The normal plan-edit API removed the nonexistent reference and required actual entity/time correlation, leaving the scope and question IDs unchanged. Preserve both `discovered-hunt.json` and `edited-hunt.json`; this is **not first-pass plan acceptance**. Approved plan version 2 SHA-256: `656e45b068d25a01980a128606e6b18f24339e280986f65eaf7b14195d4bd53f`. Job `17e31d61-7759-4719-99b4-e25e7e22f3ec` is completed; do not reenqueue it. Source-reference grounding and a blinded fixture remain open corrections.

## Trial 6 completed workflow, remaining quality failures

- Trial 6 reached `report_draft`: five completed searches cover all four approved question IDs; five execution model calls, **zero repairs**, 101,019 input / 9,431 output tokens. Both assessment batches and final synthesis passed contract/grounding checks first try. [Quality review](../runtime/qualification/quality-fixes-20260909/trial-6/quality-review.json). This is concrete workflow progress, not analytical acceptance.
- The report has three supported observations, 228 retained records, 100 distinct cited records, and zero repeated citation IDs. [Offline projection reconciliation](../runtime/qualification/quality-fixes-20260909/trial-6/fixture-projection-reconciliation.json) uniquely matches every retained row and proves **16/16 expected indicators retained and cited**. The 228 retained rows represent 220 distinct frozen events; the 100 cited records represent 92 events. The maintained direct-ID scorer correctly leaves recall unknown because the result projection omitted `event_id`; do not replace its original score with the separate reconciliation.
- **High: host ingestion/field semantics.** q2 returns four events using the default `host` field. Moving its predicate to `where` alone still returns four. Explicitly extracting the raw JSON host into a separate field, with the same source/window/host values, returns **84**, exactly matching the frozen raw-event count. The original fixture's metadata/raw-host collision hides 80 matching records. [Controlled diagnostic](../runtime/qualification/quality-fixes-20260909/trial-6/host-search-diagnostic.json). Correct/version the lab ingestion rather than interpreting this as missing endpoint activity or weakening SPL guards. Preserve the original fixture.
- **High: multi-source sampling.** q4's unchanged source/entity/time predicates match **487 distinct DNS and 480 network events**, but its truncated 100-row representative result contains **DNS only**. The workflow ends without separately investigating network events despite remaining budget. [Original aggregate comparison](../runtime/qualification/quality-fixes-20260909/trial-6/q4-source-coverage-diagnostic.json), [count correction](../runtime/qualification/quality-fixes-20260909/v8/q4-count-reconciliation.json). The original 877/855 grouped counts multiplied rows with repeated identical index/sourcetype values; they were not distinct event totals. Per-query sample balancing cannot solve source starvation inside one query. Add explicit per-source coverage and act on missing sources in truncated results; completed question IDs are not proof of answered questions.
- Authentication and DNS findings describe relationships to advisory-linked process activity but cite only their own source rows. Correlation support and routine-behavior interpretations remain subject to substantive claim review. Preserve this draft; do not finalize it as analytical acceptance.
- Next work: correct/version fixture host ingestion, address truncated multi-source coverage, bind plan intelligence references to provided sources, then test a coherent blinded fixture and the full live scenario matrix. Independent analyst acceptance remains open. All 34 hunts are preserved; no execution job is active.

## V8 source coverage and provenance correction

- V8 (prompt 1.11) schedules application-owned source-specific checks when a truncated raw representative/targeted result omits an identified requested source. It preserves the original question, event predicates, pipeline, requested dates and limits, adds an exact source constraint before the first pipe, validates the generated SPL, and checkpoints its lineage. Existing query/model/cycle budgets remain; unresolved gaps appear in model context and the report. Combined aggregates are excluded. Retained model samples also balance sources within a query.
- Evidence source identity now comes from observed projection fields and satisfiable exact source pairs, with `unknown` for ambiguous/contradictory values. Earlier code assigned every row the first proposal source. Historical evidence remains unchanged; coverage calculations use selected result fields and actual SPL.
- V8 passed 445 tests and deployed with all 34 prior hunts preserved. The [read-only replay](../runtime/qualification/quality-fixes-20260909/v8/source-coverage-live-diagnostic.json) retrieved 100 network rows with one additional search and no model call. V9 repeats this diagnostic with corrected time transport. This proves the diagnosed source-selection path; it does not qualify an entire hunt.

## Trial 7 failure and deployed v9 corrections

- Trial 7 `3f321542-07f1-4ef6-ad62-0ff421e52a98` completed to `report_draft` after an explicit plan edit: the generated reference did not exist and fixture metadata was being used for analysis. Its scope/question IDs were preserved. Approved plan v2 SHA-256: `253ee8b70994cc05b3055cc81fa41a23b8ae2485d771845d0006ebb7e7f9409d`. Preserve original/edited plans and completed job `d77da81a-dd97-4d3a-a001-dafa3fde8f91`; do not reenqueue or finalize as acceptance.
- Seven execution model calls, zero repairs, three searches, 124 retained records, 87 distinct cited records, zero duplicate citation IDs. The direct-ID scorer verifies all 16 expected indicators retained and cited. Only q1/q2 were searched under their approved IDs; an auth pivot ran separately, and DNS/network were never searched. [Scores](../runtime/qualification/quality-fixes-20260909/trial-7/scores.json), [quality review](../runtime/qualification/quality-fixes-20260909/trial-7/quality-review.json). The observed scorer exits 2 because analytical judgments remain unassessed; that is not a failed export.
- **Execution scope defect:** the executor sent `earliest`/`latest`, which the SDK forwards unchanged and Splunk ignores. The saved auth job confirms an open-ended search beginning January 1 despite a requested January 2–4 window. V9 sends documented `earliest_time`/`latest_time` UTC strings. [Controlled replay](../runtime/qualification/quality-fixes-20260909/v9/query-window-live-diagnostic.json) preserves SPL, confirms actual server bounds January 2–4, and verifies every returned timestamp within that window. Earlier proposal windows are not proof of enforced search scope. This is an application transport defect, not evidence of impossible synthetic timestamps.
- **Deferral defect:** q3/q4 were skipped pending q2/auth context, then permanently excluded even after new results arrived. V9 records `considered_query_ids` in application code, uses each question's latest decision, and reconsiders model skips after additional queries complete. Unchanged context does not loop; exact duplicate suppression and historical decisions without a recorded basis retain their behavior.
- Repeated identical source-field values now resolve to one source, while conflicting values stay unknown. The prior q4 aggregate overcount is corrected above, with original evidence preserved.
- **449 backend tests passed, no skips**, using isolated PostgreSQL; mypy passed across 55 files and image build passed. [Verification](../runtime/qualification/quality-fixes-20260909/v9/verification.json). V9 is healthy, readiness is 200, and [deployment verification](../runtime/qualification/quality-fixes-20260909/v9/deployment-verification.json) confirms all 35 hunts/jobs preserved. Image `threat-hunting:local-20260909-quality-v9-195610`, prompt 1.12/policy 1.3. Backup/prior environment are protected under `v9/`. No migration changed.
- All [2,088 original events remain unchanged](../runtime/qualification/quality-fixes-20260909/v9/fixture-verification.json). The corrected [source-check replay](../runtime/qualification/quality-fixes-20260909/v9/source-coverage-live-diagnostic.json) again retrieves 100 network records with no model call. A fresh full v9 trial, source-reference grounding, narrative claim support, realistic scenarios and independent acceptance remain open. There are 35 hunts and no active jobs.

## V10 source identity and trial 8

- V10 moved plan intelligence references out of native model output. The application assigns a hunt/content-bound ID and content digest for the actual supplied advisory field; explicit canonical references are checked at model validation, plan edit, approval, and execution. This proves supplied-content identity, not external authenticity or direct incident evidence. It passed 455 tests and preserved 35 hunts/jobs on deployment. [Verification](../runtime/qualification/quality-fixes-20260909/v10/verification.json).
- Trial 8 `63862b53-041d-4084-90ec-49548964721b` completed to `report_draft` with 10 searches covering all four approved question IDs, 12 execution model calls, zero repairs, 442,036 input / 14,806 output tokens, and 469 retained records. The automatic source check retrieved 100 network rows omitted by a truncated combined result. Source binding was valid on the generated plan; fixture-label analysis still required a normal plan edit, so first-pass plan acceptance failed. Preserve the draft and its [review](../runtime/qualification/quality-fixes-20260909/trial-8/quality-review.json).
- All 16 expected indicators were retained; only 12 were cited (75%, below the 90% threshold). Two findings cite 26 distinct records with no repeated citation IDs. [Scores](../runtime/qualification/quality-fixes-20260909/trial-8/scores.json). The scorer leaves substantive quality unassessed; its exit 2 is not an export failure.
- Deterministic [synthesis-context reconstruction](../runtime/qualification/quality-fixes-20260909/trial-8/synthesis-sampling-diagnostic.json) supplies 13 of the 16 indicators and omits six prior assessments because their full support does not fit. Three indicators were dropped by application sampling; one supplied indicator was not cited. This reconstruction is not a captured provider request. Preserve the 100-row bound and correct evidence selection/support handling rather than merely increasing budgets or adding irrelevant citations.
- Several endpoint searches returned the same four events with different projections. The report has no auth/DNS/network observation or scoped negative finding despite completed searches. Query execution coverage is improved, while answered-question coverage and efficiency remain incomplete. All 469 retained event timestamps fit their proposal windows; the first original Splunk job had expired (404) when audited, so full original request metadata is unavailable. [Time check](../runtime/qualification/quality-fixes-20260909/trial-8/query-window-verification.json).

## Frozen normalized fixture and v11 source discovery

- [Normalized v1](../tests/known_answer/fixtures/normalized-v1/README.md) is an independently defined synthetic scenario, not an independent analyst holdout. It contains 689 selected normalized events with coherent host/IP/session/process lifetimes, explicit process-start versus module-load hash targets, and separate event/collection/index times. An alternate network-outage file removes exactly 13 records. Private expectations and opaque event IDs avoid per-event answer labels. Both files were frozen before model execution and are reproducible with [the generator](../scripts/build_realistic_fixture.py).
- The complete variant is loaded once in four `th_real_v1_*` indexes and [verified against every raw event and scalar indexed metadata field](../runtime/qualification/quality-fixes-20260909/realistic-v1/load/verification.json). The original [2,088 events still match the frozen baseline exactly](../runtime/qualification/quality-fixes-20260909/v10/fixture-verification.json). No existing fixture was cleaned, renamed, deleted, or reposted. The outage variant has not been loaded or run.
- The [loader](../scripts/load_realistic_fixture.py) refuses nonempty indexes, checks index totals as well as potentially role-filtered search results, and checkpoints intent before a single HEC request. Uncertain requests require read-only verification. Provisioning corrected a delimiter mismatch in the reader's allowed-index update, then extended its existing source filter only to the four new lab indexes. Admin verification proved the first HEC request had ingested all 689 rows; no repost occurred. [Provisioning record](../runtime/qualification/quality-fixes-20260909/realistic-v1/provisioning.json).
- Trial 9 `cbde55c7-044a-4c80-be84-2e002117defb` is unapproved `awaiting_plan_review`. The app catalog used `saved/sourcetypes`, omitting all four actual indexed `lab:normalized:*` labels. The generated plan substituted old `synthetic:*` source names. The runner rejected scope mismatch before approval; preserve that [plan review](../runtime/qualification/quality-fixes-20260909/trial-9/plan-review.json).
- V11 adds one bounded `tstats` indexed-source catalog query per discovery call, using exact validated catalog/proposed indexes and existing transport/item/byte limits. It returns no raw events. Configured names remain useful metadata; failures/caps remain explicit partial discovery. No sourcetype labels or fixture data were changed to accommodate the application. The [live corrected catalog](../runtime/qualification/quality-fixes-20260909/realistic-v1/corrected-source-discovery.json) includes all four sources. `metadata type=sourcetypes` returned no rows under this reader's search filter, while documented indexed-field `tstats` returned the correct sources/counts; the implementation uses the latter.
- **483 tests passed, no skips**, including isolated PostgreSQL; mypy passed across 55 source files and image build passed. [Verification](../runtime/qualification/quality-fixes-20260909/v11/verification.json). Deployed API/worker `threat-hunting:local-20260909-quality-v11-204107` is healthy, readiness 200, and all 37 pre-existing hunts/jobs are unchanged. [Deployment](../runtime/qualification/quality-fixes-20260909/v11/deployment-verification.json). Prompt remains 1.13, policy 1.3; no migration or budget changed.
- Fresh trial 10 `9a1a6270-5e28-4583-8a81-7ad40fe23e30` completed to `report_draft` in `runtime/qualification/quality-fixes-20260909/trial-10/` against the same frozen normalized data. The plan passed first-pass review without edits, with exact source/date scope and a valid bound advisory reference. Reviewed plan SHA-256: `705a2265489e3568ff141b66efc7b44695bfb798c396f877453f29d7b78c2155`. The run qualifies only its measured checks in this small case, not enterprise realism, the outage scenario, the full twelve-case matrix, or independent analytical acceptance.

## Trial 10 outcome and next correction boundaries

- Trial 10 completed five searches covering all four approved question IDs with **five execution model calls, zero repairs**, 117,831 input / 9,282 output tokens. One combined result omitted DNS; the application-owned source check retrieved it. An identical adaptive endpoint pivot was correctly suppressed. [Quality review](../runtime/qualification/quality-fixes-20260909/trial-10/quality-review.json).
- The expected advisory hash event is retained and cited (**1/1**). All 256 retained projections match the frozen fixture and source metadata; they represent 251 distinct events. The single finding cites 98 distinct records representing 94 events, with no repeated citation IDs. [Evidence verification](../runtime/qualification/quality-fixes-20260909/trial-10/evidence-verification.json), [scores](../runtime/qualification/quality-fixes-20260909/trial-10/scores.json). All five original Splunk job request time parameters were captured and match their proposals; every retained event time is in its respective query window. [Job-time checkpoints](../runtime/qualification/quality-fixes-20260909/trial-10/query-window-checkpoints.json).
- Core observations distinguish the one hash match from same-name files and do not assert confirmed ransomware or attribution. Narrative quality still requires revision: “normal”/“routine” wording overstates what the selected session/process/connection records establish. All 51 cited DNS/network records belong to `browser.exe`, and 28 are outside the five sessions with observed HRsword starts; the report should explicitly distinguish host/account context from process/session relationships. Do not change the frozen data to eliminate this ambiguity or manually edit the original report into a passing model result.
- V12 below corrects the diagnosed synthesis support selection loss. Next application work: assess redundant query semantics and improve claim-level observation/inference discipline. Preserve budgets and failure artifacts. V11's `tstats_available` advisory flag still reflects the older REST-only metadata path even when indexed-source probing succeeds; reconcile that context fact in the next relevant correction.
- The 689-event case is deliberately smaller than the original 16-indicator scenario. Its lower model usage is not a controlled data-only estimate of improved reliability. The outage case, full live matrix, and independent analyst review remain open. All 38 hunts are preserved and no execution job is active. [Final database state](../runtime/qualification/quality-fixes-20260909/v11/final-database-state.json).

## V12 synthesis support correction and bounded model diagnostic

- V12 (prompt 1.14) first reserves representation for each observed source/query within the existing 100-row synthesis cap. It then includes complete assessment support groups in approved-question order before adaptive groups, and fills remaining capacity with balanced samples. Oversized groups or unavailable/wrong-query references receive no partial priority allocation. Assessments still require all support to be supplied and remain labeled interpretations. This uses recorded question/source relationships and validated support; it never reads fixture expectations or assumes maliciousness. Assessment-step sampling, budgets, canonical storage contracts and frozen data are unchanged.
- A new multi-query regression reproduced the failure before the correction. **485 backend tests passed with no skips**, including isolated PostgreSQL; mypy passed across 55 files and image build passed. [Verification](../runtime/qualification/quality-fixes-20260909/v12/verification.json). The healthy [v12 deployment](../runtime/qualification/quality-fixes-20260909/v12/deployment-verification.json) preserves all 38 hunts/jobs exactly. Backend/worker image: `threat-hunting:local-20260909-quality-v12-210530`; protected backup/prior environment are under `v12/`. No migration changed.
- The [deterministic before/after replay](../runtime/qualification/quality-fixes-20260909/v12/trial-8-synthesis-selection-replay.json) reconstructs context from the same preserved trial-8 final checkpoint. Both versions supply 100 rows. Indicator events supplied improve from 13/16 to 16/16. Five complete approved-question support groups use 96 rows; four remaining rows represent the other nonempty queries. The four adaptive assessments still lack full support and are explicitly omitted, compared with six omitted assessments previously. This is a deterministic selection check, not a captured original provider request or a completed hunt.
- A [separate one-call synthesis diagnostic](../runtime/qualification/quality-fixes-20260909/v12/trial-8-model-diagnostic/result.json) with the same model/settings and corrected selection passed schema/reference/materialization checks with zero repairs, 46,197 input / 1,999 output tokens. Its three findings cite all 16 expected indicators and include auth/DNS/network observations. They cite 96 distinct evidence records representing 90 events, with zero repeated citation IDs within findings. The original hunt/report were not resumed or rewritten; this diagnostic does not change its recorded 75% citation recovery.
- [Narrative review still fails](../runtime/qualification/quality-fixes-20260909/v12/trial-8-model-diagnostic/quality-review.json): familiar-looking activity is called routine/expected without a supplied baseline, observed executions are called deliberate without evidence of intent, and a positive observation blends into an unsupported broad absence-of-C2 conclusion. Eight cited records in the execution-cluster finding represent four events because different queries retained duplicates. Corrected reference selection does not establish factual quality, and a single diagnostic does not establish repeatable model improvement. These are application/report issues; the frozen fixture should not be changed to remove the ambiguity.
- No fresh complete v12 hunt has run. Claim-level discipline, redundant query/event support, the outage case, full live matrix, independent analyst review, and the existing `tstats_available` context discrepancy remain open. There are 38 hunts and no active jobs. [Final database state](../runtime/qualification/quality-fixes-20260909/v12/final-database-state.json).

## V13 claim guidance and trial 11 review

- V13 (prompt 1.15) passed **490 backend tests/no skips** and mypy, and deployed while preserving all 38 prior hunts. It carried observation/inference descriptions into assessment/synthesis native schemas and repairs; successful scoped tstats probes now establish availability even when they return no rows. [Verification](../runtime/qualification/quality-fixes-20260909/v13/verification.json).
- Fresh trial 11 reached `report_draft`: six completed searches, five execution model calls, zero repairs, 141,590 input / 13,851 output tokens. [Review](../runtime/qualification/quality-fixes-20260909/trial-11/quality-review.json). All 262 retained projections reconcile against the frozen fixture, representing 248 events; 100 cited records represent 92 events and include the one expected indicator. All six server query windows matched their proposals. [Evidence verification](../runtime/qualification/quality-fixes-20260909/trial-11/evidence-verification.json), [scores](../runtime/qualification/quality-fixes-20260909/trial-11/scores.json).
- Factual review fails: the ops-03 binary matches the filename but not an advisory hash; no observed full paths were supplied by the advisory. The report nevertheless claims both hashes/paths match. Other claims infer normal sessions/work hours and HTTPS/TLS without sufficient support. These are not fixture realism defects.
- Another deterministic application defect is confirmed: the auth query uses `search | table` but the model labels its result mode `aggregate`. Evidence kind follows that declared mode, so all 16 raw auth rows are excluded from point-time bounds. [Diagnostic](../runtime/qualification/quality-fixes-20260909/v14/result-kind-diagnostic.json). Correct the source of result-kind classification rather than editing historical evidence or requiring customer query maintenance.

## V14 IOC comparisons and matched-input reasoning diagnostic

- The assessment step previously had query results but no extracted advisory IOC values. V14 passes the existing bounded IOC lists to assessment and synthesis and computes comparisons beside each sampled row. Filename-only matches, observed nonmatching hash literals, and missing comparable hash literals remain distinct. Hash case is normalized only for comparison; original values and source records remain unchanged. No full STIX evaluator, schema-mapping configuration, or customer preparation step is added.
- **496 backend tests passed/no skips**, mypy passed across 55 files, and image build passed. Initial failures from an incorrect local variable and a stale prompt assertion were corrected; their log is retained. [Verification](../runtime/qualification/quality-fixes-20260909/v14/verification.json). [Deployment](../runtime/qualification/quality-fixes-20260909/v14/deployment-verification.json) verifies readiness 200 and all 39 pre-existing hunts/jobs unchanged. Image `threat-hunting:local-20260909-quality-v14-214413`, prompt 1.16, policy 1.3.
- The five-row [deterministic replay](../runtime/qualification/quality-fixes-20260909/v14/ioc-comparison-replay.json) distinguishes the one exact advisory hash from four filename-only matches. The separate [two-call diagnostic](../runtime/qualification/quality-fixes-20260909/v14/trial-11-model-diagnostic/result.json) reassesses that query and synthesizes with the other preserved assessments: both calls validate without repairs and preserve the correct match distinction. Remaining routine/HTTPS overclaims fail review.
- A separate [medium-reasoning diagnostic](../runtime/qualification/quality-fixes-20260909/v14/trial-11-medium-diagnostic/result.json) uses byte-identical synthesis context/system text and the same native schema. It validates in one call, with 62,497 input / 7,466 output tokens, but still calls TCP-443 traffic HTTPS and misattributes some browser events to HRsword-containing sessions. The none-reasoning synthesis used 62,497 / 2,959 tokens. One call per setting is not repeatability evidence; neither qualifies factual quality. No deployed model setting was changed.
- Fresh trial 12 `5b549b2d-400f-42c2-93eb-c6074eee0e36` completed to `report_draft` after an unchanged first-pass plan review. Six queries, six execution model calls, one successful FollowUpDecision schema repair, 158,600 input / 14,747 output tokens. The safe repair trace does not identify the original failed field; do not invent that diagnosis. [Plan review](../runtime/qualification/quality-fixes-20260909/trial-12/plan-review.json), [scores](../runtime/qualification/quality-fixes-20260909/trial-12/scores.json).
- The first assessment and final findings now correctly distinguish the advisory hash match from the filename-only matches. All 281 retained projections reconcile against the unchanged fixture and actual server query windows; they represent 266 events. The 84 cited records represent 80 events, include all four sources, and recover the one expected indicator. [Verification](../runtime/qualification/quality-fixes-20260909/trial-12/evidence-verification.json).
- [Factual review still fails](../runtime/qualification/quality-fixes-20260909/trial-12/quality-review.json): the browser finding claims shared HRsword sessions even though 31 cited DNS/network records belong to other ws-17 sessions. It also infers HTTPS/vendor resource ownership; another finding says a process terminated normally without an exit status. The raw-auth/aggregate classification defect repeats. Preserve this draft and do not requeue or finalize it as acceptance.
- [Final database verification](../runtime/qualification/quality-fixes-20260909/v14/final-database-state.json) confirms 40 hunts, no active execution jobs, and all 39 prior hunt/job records unchanged. Full live matrix, outage behavior, customer operating burden, and independent analyst acceptance remain open. Next concrete work is application-owned result-kind classification and reliable relationship/claim support; no customer normalization or prompt-maintenance requirement is acceptable.

## September 9 result-kind correction and rejected relationship-context experiment

- V15 derives `raw_event` versus `aggregate_row` from the executed supported SPL pipeline. The requested result mode still controls existing retrieval limits. A truncated raw search requested in aggregate mode now receives the same omitted-source checks as other raw searches. This adds no customer configuration or dependencies.
- Five regression cases failed before the correction. A replay of trial 12's 16 authentication rows now classifies them as raw events and computes their observed timestamp bounds, preserving every selected source value. Historical rows and the failed draft are unchanged. [Result-kind replay](../runtime/qualification/quality-fixes-20260909/v15/result-kind-replay.json).
- An additional candidate supplied exact host/session and host/process groups, with missing/conflicting identities kept separate, plus field guidance for missing protocol, normal-exit and ownership facts. Deterministic checks correctly separated all 31 falsely linked records and preserved the ten-row same-process control. However, the actual model still repeated all 31 wrong-session citations, HTTPS, and normal-session claims. [Factual review](../runtime/qualification/quality-fixes-20260909/v15/model-diagnostic/quality-review.json).
- That context/instruction candidate was removed from production. Its source snapshots, tests, and exact provider requests/responses remain under `v15/rejected-context-candidate` and `v15/model-diagnostic`. The diagnostic runner depends on that saved candidate, not the final deployed code. One synthesis call per case, no repairs: failed-run replay 59,877 input / 2,383 output tokens; control 10,198 / 824. The control supports the scoped process relationship and zero-result conclusion, but leaks request-local citation labels into prose. Neither case is a complete hunt or independent analyst acceptance.
- [Deployment verification](../runtime/qualification/quality-fixes-20260909/v15/deployment-verification.json) confirms healthy API/worker and preserved database state. [Installed-code replay](../runtime/qualification/quality-fixes-20260909/v15/installed-replay.json) passes in both containers; both frozen fixtures remain unchanged. No fresh full hunt was run after the failed context experiment.
- The final smaller change passes **500 backend tests with isolated PostgreSQL and no skips**, plus mypy across 55 source files. The discarded context candidate passed 503 tests; that count is not the final release's test count. [Final test log](../runtime/qualification/quality-fixes-20260909/v15/backend-tests-final.txt).
- Relationship/claim support remains the next quality task. Correct relationship metadata alone did not prevent false prose. Evaluate an explicit, application-checkable claim contract or bounded factual-verification step against the preserved failure and supported control before running another full hunt. Do not expand customer setup, normalize fixtures for a passing score, repeatedly rerun the same candidate, or declare prompt compliance based on valid citations.

## September 9 source-based final handoff, quota diagnosis, and search ceiling (v18)

The authoritative database has 41 hunts and no queued/claimed jobs. Recent live qualification comprises the September 8 baseline plus trials 1–13, excluding unapproved trials 2 and 9: **12 executed hunts, 9 reports (75%), and 3 execution failures**. The latest seven executed trials all produced reports. All nine reports fail at least one factual or completeness check; there are **zero fully accepted hunts**. These are changing development configurations, not a repeatability estimate for one released version. [Database snapshot](../runtime/qualification/quality-fixes-20260909/v18/live-qualification-status.json).

Trial 13 review verifies **439 raw records / 258 distinct fixture events**, with zero selected-field mismatches; the eight authentication aggregate rows reconcile with the frozen fixture. All twelve captured server windows match the actual query bounds. Its main exact-hash execution/session finding is supported. Its report omits the filename-only HRsword.exe activity on ops-03 and relevant browser communications sharing the lead's session. The authentication aggregate groups by fields absent on logoff events and therefore represents logons only; later raw searches supply logoffs. This is a query-coverage limitation, not impossible synthetic data. Verification is against preserved records, fixture values, and captured windows; it does not claim a fresh independent fetch of expired original Splunk job rows. [Review](../runtime/qualification/quality-fixes-20260909/trial-13/quality-review.json).

V18 removes prior assessment/skip prose and its whole-citation-group sample priority from final synthesis. Exact extracted IOC literals are prioritized within query/source-balanced sampling. The total cap stays 100 records. On the same trial 13 data, the later authentication and communications queries each receive **10 records instead of 1**, and the later endpoint query receives 10 instead of 1. Assessments remain in the investigation/audit history. Tests cover invariance of final context under false assessment prose, different citation groups, and invented skip explanations; source values remain unchanged. This is an initial handoff correction. Per-question/claim evidence sets, targeted retained-record retrieval, and semantic-support validation remain unfinished.

The first focused run passed 65 tests but exposed a test input using a plain filename where the existing extractor requires a STIX file-name pattern. Correcting the test to the documented input shape produced 66 passing focused checks; no fixture or parser was changed. The complete backend suite passed 513 tests before the quota-classification addition. Final combined verification: **522 backend tests passed with isolated PostgreSQL/no skips**, 19 focused quota/error tests passed, mypy passed across 55 source files, and diff checks passed. The funded comparison below completed after the user restored capacity; deployment status is recorded separately. [Verification](../runtime/qualification/quality-fixes-20260909/v18/verification.json).

A controlled old/new-context synthesis replay and its positive control all failed at the provider boundary; no model verdict or accuracy comparison was obtained. A separate minimal provider diagnosis returned **`credit_balance_exhausted` / `insufficient_quota`**, with no retry-after header. The user first requested a hold, then confirmed additional funds and authorized resuming calls. The funded comparison completed successfully at the provider boundary. Do not purchase credits or use an unrelated Codex usage reset. [Provider diagnosis](../runtime/qualification/quality-fixes-20260909/v18/provider-limit-check.json), [failed comparison](../runtime/qualification/quality-fixes-20260909/v18/synthesis-ablation/result.json).

The local adapter correction adds `provider_quota_exhausted` as a distinct nonretryable category using allowlisted structured provider codes. Other 429 responses retain temporary rate-limit handling. Tests use an injected SDK error to check one attempted call, zero repairs, and no credential-bearing provider message in diagnostics. The first quota test used an unsupported dynamic object schema and failed before reaching the adapter; it was corrected to the existing HuntPlan contract without changing runtime schema policy.

The funded replay produced first-pass schema/reference-valid baseline, candidate, and positive-control responses with zero repairs. Both baseline and candidate now include the second-host filename match; a single sample does not establish a causal accuracy gain. The candidate describes same-session browser communications without attributing them to HRsword, but does not provide a complete sourced communications answer. The positive control still calls observed TCP/443 HTTPS without application-protocol evidence. This is an unsupported claim relative to the supplied evidence, not proof that additional telemetry could not resolve it. [Funded factual review](../runtime/qualification/quality-fixes-20260909/v18/synthesis-ablation-funded/quality-review.json).

At the user's request, the next candidate raises the per-hunt search ceiling from 12 to **50**. Code defaults, shipped YAML, README, specification, and runbook agree. Existing 20-minute timing, query concurrency, 12 model calls, and token/evidence budgets remain independent. Verification after this change: **522 backend tests with isolated PostgreSQL/no skips**, mypy across 55 files. Existing approvals retain their original settings; qualification uses a fresh plan.

The user challenged the assumption that errors necessarily reflect bad reasoning over sufficient evidence. Evaluation must separate evidence not retrieved, retrieved evidence omitted from model context, and conclusions unsupported by the provided context. Additional searches may resolve the first case; a larger search cap alone does not fix the 100-record handoff. Capture new queries and source results, reconstruct the model sample with the installed code, and inspect which material records reach synthesis. Do not claim that an increased cap or one successful response proves accuracy. Keep the full goal active.

The evidence trace confirms an intentional **hardcoded 100-record model-context cap**, independent of the configured representative/targeted retrieval limits. All 447 trial-13 records remain stored, but 347 are excluded from final synthesis. Its new 100-record sample contains eight aggregates and 92 raw-record representations covering 63 distinct events. Final synthesis has no retained-evidence retrieval loop. Earlier sampling priority is corrected, but this fixed handoff remains incomplete. Trial 14 is prepared and not executed while a matched 100-versus-447-record diagnostic isolates this issue without new searches or data edits. [Trace](../runtime/qualification/quality-fixes-20260909/v18/evidence-handoff-trace.json).

The omitted-record review confirms relevant loss: the exact-lead session has 52 retained DNS/network records but only 10 supplied, ending at 09:19:05, before the indicator process start at 13:07:43. All 20 retained later communications are omitted from the current final sample. These are browser-process records in the same session, useful for surrounding activity but not proof of HRsword communications or spread. [Concrete omitted evidence](../runtime/qualification/quality-fixes-20260909/v18/omitted-relevant-evidence.json).

The 100-versus-447-record diagnostic did not yield an expanded-context verdict: the 447-record request received HTTP 400 before generation. Its schema has 1,389 enum choices because citation enums are copied across three classification branches, exceeding the documented 1,000-choice schema limit. Preserve that failure; reuse citation definitions without weakening validation before retrying expanded evidence. Do not infer an accuracy result from this request failure. [Diagnostic review](../runtime/qualification/quality-fixes-20260909/v18/evidence-volume-diagnostic/quality-review.json).

## September 9 hunt timing correction (v17)

User-requested change: raise total hunt execution from 12 to **20 minutes**, and synchronize related behavior. The per-model-call timeout remains **300 seconds** in the deployed configuration; the query timeout remains 120 seconds, model-call count 12, and existing token/evidence budgets unchanged.

The review found that the old relative query cutoff and synthesis reserve existed in configuration but were not enforced by execution. V17 derives and enforces the cutoff: with 300-second model calls, stop new searches/adaptive work at minute **13**, allow in-flight searches through minute **15** subject to each search's original 120-second timeout, and reserve five minutes for synthesis inside the 20-minute ceiling. Worker recovery preserves hunt and query start times. Skipped searches and timeouts remain explicit coverage gaps, and late model responses are accounted for without accepting their content. Configurations omitting the model timeout retain the generic 120-second default and therefore a minute-16 cutoff; short custom durations clamp reserves to fit. The obsolete time-of-day cutoff is accepted for old YAML files but is not used.

Timing verification: **513 backend tests passed**, with isolated PostgreSQL and no skips; mypy passed across 55 source files. Simulated clocks cover work after the former 12-minute limit, cutoff-driven synthesis, final calls shortened by the remaining deadline, rejection at minute 20, and the original per-query timeout on recovery. The first targeted run exposed an incorrectly shaped fake usage response in a new test; correcting the fake to its documented nested usage contract made the accounting assertion pass. Build and deployment passed. Both installed services report 1,200 seconds total, 780 seconds to query cutoff, 120 seconds for in-flight queries, and 300 seconds for synthesis/model calls; installed workflow/executor hashes match tested source. All 41 hunts/jobs and frozen fixture hashes are unchanged. See [deployment verification](../runtime/qualification/quality-fixes-20260909/v17/deployment-verification.json) and [verification record](../runtime/qualification/quality-fixes-20260909/v17/verification.json).

The v16 **90-second** runs were deliberately shorter diagnostics, not equivalent to a five-minute deployed call. Preserve them as historical observations. A matched GPT-5.1 medium review rerun used the 300-second timeout read from the active configuration, preserving the old input, schema, prompt, and diagnostic output-token allowance to isolate the time-limit difference. It returned four issues in approximately **95.6 seconds** (artifact-write timestamps), beyond the former limit. It detected unsupported normal termination, incorrect session attribution, and unsupported HTTPS; vendor-ownership claims remain unflagged. Its positive control received a provider **429 rate limit**, not a verdict. This supports correcting the diagnostic cutoff but does not qualify a factual-review gate. See the [diagnostic review](../runtime/qualification/quality-fixes-20260909/v17/review-medium-timeout-parity/quality-review.json). This is a diagnostic, not a new complete hunt or a current-model qualification. Future end-to-end qualification uses the deployed application's immutable approval settings.

The timing change does **not** resolve these analytical work items:

- Replace broad shared evidence samples with bounded sets tied to individual questions/claims, automatically retrieve relevant omitted records, and retain explicit counts/source coverage and query/sample truncation. Deterministic filtering and correlation need to preserve original source values and ambiguous identities. Customers must not assemble these sets or normalize records manually.
- Earlier assessments are currently labeled as interpretations and filtered to supplied citations, but their prose still enters final synthesis. Evaluate a final-writing path that uses source records, query scope, and typed support relationships without earlier conclusion prose. Check source-supported host/session/process relationships in code where semantics permit, and keep uncertain causal/behavioral claims as hypotheses. The proposed model critic is not yet a reliable production gate: its false positives and misses are preserved in v16.
- Product-owned skills/playbooks are a possible way to load relevant procedures. Merely relocating identical prompt text into Markdown does not establish accuracy gains. Compare identical inputs/models/budgets over repeated realistic trials before adopting a procedural change; no customer-authored skills or rule tuning are required.

## September 9 model and factual-review evaluation (v16)

- A separate bounded review prototype asks for exact unsupported passages and their evidence/query references. It does not alter production workflow or add customer work. GPT-5.1 with reasoning `none` catches the false session and normal-termination claims, but invents a problem with the correctly recorded process ID across four dates and misses HTTPS/ownership claims. The supported process/zero-result control receives no flags. [Review evaluation](../runtime/qualification/quality-fixes-20260909/v16/review-diagnostic/quality-review.json).
- A matched-input GPT-5.1 `medium` review reaches the diagnostic's **90-second** limit without a verdict. The subsequent control is rejected locally because that outstanding call still occupies adapter capacity; it is not a second provider result. Usage is unavailable for the timed-out request, not known to be zero cost. The deployed application already allows **300 seconds per call, 12 minutes per hunt, and 12 model calls**; these limits were not increased. [Timeout record](../runtime/qualification/quality-fixes-20260909/v16/review-medium-diagnostic/quality-review.json).
- [Official GPT-5.6 Terra documentation](https://developers.openai.com/api/docs/models/gpt-5.6-terra) establishes Chat Completions, native structured outputs, and medium reasoning support; the existing account's model endpoint confirms access. No SDK, dependency, or API transport change is needed.
- With identical review context/system/schema, Terra at medium reasoning catches the session, HTTPS, and normal-termination errors and accepts the supported control. It also challenges the tentative locally-installed-tool inference too strongly. The reviewer remains unqualified as an automatic gate; no review stage was added. [Review evaluation](../runtime/qualification/quality-fixes-20260909/v16/review-terra-diagnostic/quality-review.json).
- Terra then uses the existing production synthesis prompt and preserved trial-12 evidence/assessments, with only the already-fixed row-kind projection. Its three findings correctly distinguish the advisory hash, filename-only observations, and actual authentication/session relationship. They cite seven distinct records including the expected indicator. Source values, identifiers, dates, paths, and account literals were checked directly. A separate model review reports no issues. Synthesis uses 56,543 input / 938 output tokens; diagnostic review uses 48,865 / 236. This is one saved-evidence replay, not a complete hunt, representative quality proof, or independent analyst acceptance. [Factual review](../runtime/qualification/quality-fixes-20260909/v16/terra-synthesis-diagnostic/quality-review.json).
- The local evaluation configuration now uses `gpt-5.6-terra`, reasoning `medium`, with the unchanged v15 image/prompt 1.17 and unchanged budgets. Healthy API and worker settings were verified inside both containers; all 40 prior hunt/job records were preserved. [Deployment](../runtime/qualification/quality-fixes-20260909/v16/deployment-verification.json), [loaded settings](../runtime/qualification/quality-fixes-20260909/v16/installed-model-settings.json).
- Fresh trial 13 `e98ad766-9dc8-4ab6-a727-ec6a3cf6b72a` has an unchanged first-pass reviewed plan covering the exact frozen scope and four required questions. Plan SHA-256 `5f386b02232ce803ded98b21bad5859ffa90e2d54637bfbf0f5df8fa5f267a33`. Execution was enqueued once. [Plan review](../runtime/qualification/quality-fixes-20260909/trial-13/plan-review.json). Inspect this same hunt/job for its current state; do not requeue it or restart services while it is active.
- The same explanatory synthetic fixture input is retained for a fair model comparison. Success here would not prove native-source handling without such explanation, effortless customer onboarding, outage behavior, the full twelve-case matrix, or independent acceptance. Those requirements remain open.

## User's synthetic-data realism requirement

The user explicitly permits realism corrections, but rejects changing data merely to make the application pass. Preserve the frozen baseline and failed runs. Make each new fixture change defensible from the intended event semantics and an explicit scenario, independently of model success.

- Missing fields, inconsistent labels, duplicated extraction values, collector-default hosts and misconfigured timestamps can occur in real pipelines. Keep appropriate cases as documented robustness scenarios; do not classify all messy telemetry as impossible or remove it to improve scores.
- Define field meanings before changing process/file mismatches or DLL execution records. Validate coherent entity identities, process/image relationships, lifecycle and event ordering, and host/IP/account/time relationships. Distinguish event time from collection/index time and document any clock skew or collection defect.
- Keep fixture answer keys and scenario labels out of analyst-visible event data. Expected outcomes belong to the evaluator. Use both supported investigations and deliberately incomplete telemetry, with expected uncertainty or missing coverage where justified.
- Do not rewrite baseline evidence, normalize suspicious values solely because a query misses them, or treat an improved fixture score as proof that a workflow defect was fixed. Retain the current data for controlled application comparisons, and label realistic variants separately.

## Customer operating burden requirement

The September 9 user instruction is explicit: getting the app working must not require heavy customer operations. Standard integration access, permissions, and hunt scope are reasonable inputs. Schema discovery, supported source interpretation, IOC comparisons, query handling, evidence bookkeeping, and routine validation belong to the application. Do not require customers to normalize telemetry by hand, maintain prompt rules, author correlation logic, or supply bespoke baselines merely to avoid incorrect claims. Optional organization context can improve an investigation; absent context must produce a scoped unknown or limitation rather than invented certainty or mandatory setup work. Evaluate each remaining fix against this requirement, including the analyst/deployer journey. The v14 IOC comparison and v15 result-kind corrections introduce no customer configuration.

## Resume artifacts and tools

- September 9 [baseline scores](../runtime/qualification/quality-fixes-20260909/baseline-scores.json) reproduce 16/16 retrieved, 13/16 cited, 68 distinct citations, 11 duplicate references, and explicit question-ID execution coverage of 1/4. Authentication pivots occurred, but only q1 among the four original question IDs had a completed query; this metric does not infer semantic answers.
- [Quality review and exact omitted indicator IDs](../runtime/qualification/live-e2e-20260908/quality-review.json)
- [Original hunt results](../runtime/qualification/live-e2e-20260908/hunt-results.json)
- [Final report PDF — failed quality review](../runtime/qualification/live-e2e-20260908/final-report.pdf)
- [Final report content](../runtime/qualification/live-e2e-20260908/api-finalized-report.json)
- [Verified fixture](../runtime/qualification/live-e2e-20260908/verified-fixture.json), [frozen current events](../runtime/qualification/live-e2e-20260908/current-lab-events.json), [fixture manifest](../runtime/qualification/live-e2e-20260908/fixture-manifest.json)
- [Splunk jobs](../runtime/qualification/live-e2e-20260908/splunk-job-verification.json), [evidence roundtrip](../runtime/qualification/live-e2e-20260908/evidence-roundtrip-verification.json), [PostgreSQL/audit checks](../runtime/qualification/live-e2e-20260908/database-audit-verification.json), [model-call metadata](../runtime/qualification/live-e2e-20260908/live-model-call-metadata.json)
- Browser screenshots, HTTP status checks, and PDF inspection output are in the same `runtime/qualification/live-e2e-20260908/` directory. Runtime artifacts are local and ignored by Git.
- Test scripts are preserved in that directory under `scripts/`. They describe this run and contain fixed paths/IDs; **do not blindly rerun setup, execute, or finalize against this already-finalized hunt**.
- Deployment checks must treat queue statuses `queued` and `claimed` as active. Use `runtime/qualification/quality-fixes-20260909/v6/database-state.sql`; the older v3 helper incorrectly checked `running` instead of `claimed`. Do not restart services while either active state exists.
- Locked Python test environment: `/tmp/threat-hunting-locked-env`; uv: `/tmp/threat-hunting-build-tools/bin/uv`. Recreate if temporary paths disappear.
- September 9 tests use an isolated native PostgreSQL cluster under `/tmp/threat-hunting-quality-pg`, with a local socket in `/tmp`, port 55439, and database `threat_hunting_quality_test`. Revalidate the process before reuse; temporary paths can disappear. Never use the app database as a pytest target.
- Headless browser used existing Playwright from `/home/dreyna/src/ai-enabled-threathunting/frontend/node_modules/playwright`; that other checkout supplied tooling and versioned fixtures only. The tested app was this workspace's deployed image.
- Protected temporary test credentials are at `/tmp/threat-hunting-e2e-auth.json`; do not print or copy their contents into documentation. Browser and API sessions used for completed stages were logged out. Create another dedicated account if the temporary credential file is unavailable.
- Rollback image tags: `threat-hunting:rollback-20260908-8c6568542268` and `threat-hunting-frontend:rollback-20260908-d35be4473aae`. Protected pre-deploy database backup: `runtime/qualification/hardening/pre-deploy-database.dump`. Follow the [operator runbook](operator-runbook.md); preserve volumes.

## Other acceptance/release work still open

- Representative independent analyst acceptance and the full twelve-scenario live qualification; today's one full hunt is not that matrix.
- The standard twelve-scenario runner still requires an application-owned fixture adapter. The new single-trial API runner provides repeatable execution/capture through existing gates; scenario loading and the full live matrix remain open.
- Remote CI execution after an authorized commit/push; CI was configured and equivalent local checks passed, but remote CI has not run.
- Application rollback to the September 8 hardening image and return to the candidate passed on September 9 with health and all 28 existing hunts preserved. Both releases use migration 0013, so no schema downgrade was required. Future schema-changing releases still require their own rollback qualification.
- Production TLS/HTTPS, least-privilege external accounts, approval-reference validation, and pinned base-image digests. Backend package dependencies are already locked.
- MCP/refactor work remains separate from this immediate quality-fix block; see the [prior refactor plan](agentic-threat-hunting-refactor-plan.md).
- Routine upstream deprecation-warning maintenance. The generic frontend E2E command is still a placeholder; today's browser run was a separate live verification script.

Docker engine recovery is verified for this session. Check the engine, container health, and exact Splunk endpoint before a live trial; a stale runtime socket is an environment failure, not a hunt-quality result.
